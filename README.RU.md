# Candle Video

[![License](https://img.shields.io/badge/license-Apache--2.0-blue.svg)](LICENSE)
[![Rust](https://img.shields.io/badge/rust-1.70+-orange.svg)](https://www.rust-lang.org/)
[![RU English](https://img.shields.io/badge/Русский-red)](README.RU.md)
[![EN English](https://img.shields.io/badge/English-blue)](README.md)

Интеграция модели **LTX-Video** для фреймворка [Candle](https://github.com/huggingface/candle) на языке Rust.

Этот проект реализует высокопроизводительный инференс модели генерации видео LTX-Video, обеспечивая скорость генерации быстрее реального времени на современных GPU.

> ⚠️ **Альфа-стадия**: Проект находится в **альфа-стадии**. Корректный инференс **не гарантируется**. API может изменяться, результаты могут быть нестабильными или некорректными. Используйте на свой риск.

## 🎯 Возможности

- **Text-to-Video (T2V)**: Генерация видео из текстового описания
- **Image-to-Video (I2V)**: Анимация статического изображения
- **Высокая производительность**: Оптимизированная реализация на Rust с поддержкой CUDA
- **Экономия памяти**: Поддержка Flash Attention 2 для эффективной работы с большими последовательностями
- **Гибкие форматы данных**: Поддержка F32, BF16 и FP8
- **Rectified Flow**: Современный планировщик диффузии для быстрой генерации

## 📋 Требования

### Системные требования

- **Rust**: версия 1.70 или выше
- **CUDA**: версия 11.8 или выше (для GPU-ускорения)
- **Git LFS**: для работы с большими файлами моделей
- **Visual Studio Build Tools** (Windows): для компиляции CUDA-ядер

### Установка Git LFS

```bash
# Установка Git LFS (если еще не установлен)
git lfs install
```

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone https://github.com/FerrisMind/candle-video.git
cd candle-video
```

### 2. Сборка проекта

#### Сборка с Flash Attention (рекомендуется)

```bash
# Windows
build_flash_attn.cmd

# Linux/macOS
cargo build --release --features flash-attn
```

#### Сборка без Flash Attention

```bash
cargo build --release
```

### 3. Загрузка модели

Модель LTX-Video должна быть размещена в директории `ltxv-2b-0.9.8-distilled/` в корне проекта.

Структура директории:
```
ltxv-2b-0.9.8-distilled/
├── ltxv-2b-0.9.8-distilled.safetensors
├── model_index.json
├── t5-v1_1-xxl-encoder-Q5_K_M.gguf
└── vae/
    └── vae.safetensors
```

### 4. Генерация видео

#### Text-to-Video

```bash
cargo run --release --bin ltx-video -- \
    --prompt "A beautiful sunset over the ocean with waves crashing on the shore" \
    --model-path ./ltxv-2b-0.9.8-distilled \
    --output output_video \
    --num-frames 17 \
    --height 512 \
    --width 768 \
    --num-inference-steps 20 \
    --guidance-scale 7.5 \
    --seed 42
```

#### Image-to-Video

```bash
cargo run --release --bin ltx-video -- \
    --mode i2v \
    --prompt "A serene landscape with gentle movement" \
    --image path/to/your/image.jpg \
    --model-path ./ltxv-2b-0.9.8-distilled \
    --output output_video \
    --num-frames 17
```

## 📖 Использование

### CLI Параметры

Основные параметры команды `ltx-video`:

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `--prompt` | Текстовое описание видео | *(обязательно)* |
| `--model-path` | Путь к директории с моделью | *(обязательно)* |
| `--output` | Директория для сохранения кадров | `output` |
| `--mode` | Режим генерации: `t2v` или `i2v` | `t2v` |
| `--image` | Путь к изображению (для i2v) | - |
| `--num-frames` | Количество кадров (8N+1: 9, 17, 25...) | `17` |
| `--height` | Высота видео (кратна 32) | `512` |
| `--width` | Ширина видео (кратна 32) | `768` |
| `--num-inference-steps` | Количество шагов инференса | `20` |
| `--guidance-scale` | Масштаб классификатор-фри гида | `7.5` |
| `--frame-rate` | Частота кадров для RoPE | `25.0` |
| `--seed` | Случайное зерно | `42` |
| `--use-flash-attn` | Использовать Flash Attention | `false` |
| `--f32` | Использовать FP32 точность | `false` |
| `--fp8` | Использовать FP8 точность | `false` |
| `--cpu` | Использовать CPU вместо GPU | `false` |

### Программный API

```rust
use candle_video::{
    config::{DitConfig, InferenceConfig, SchedulerConfig, VaeConfig},
    pipeline::{PipelineConfig, TextToVideoPipeline},
    text_encoder::T5TextEncoderWrapper,
};
use candle_core::Device;

// Создание конфигурации
let config = PipelineConfig {
    dit: DitConfig::default(),
    vae: VaeConfig::default(),
    scheduler: SchedulerConfig::default(),
};

// Инициализация пайплайна
let device = Device::Cpu; // или Device::cuda_if_available(0)?
let pipeline = TextToVideoPipeline::new(&device, config)?;

// Конфигурация инференса
let inference_config = InferenceConfig {
    num_frames: 17,
    height: 512,
    width: 768,
    num_inference_steps: 20,
    guidance_scale: 7.5,
    frame_rate: Some(25.0),
    ..Default::default()
};

// Генерация видео
let video_frames = pipeline.generate_with_cfg(
    &text_embeddings,
    &inference_config,
    &negative_embeddings,
)?;
```

## 🏗️ Архитектура

Проект реализует полный пайплайн генерации видео:

```
Text Prompt → T5 Encoder → Text Embeddings
                                    ↓
         Random Noise → DiT Denoising Loop → Denoised Latents
                                    ↓
                           VAE Decoder → Video Frames
```

### Основные компоненты

- **T5 Text Encoder**: Кодирование текстовых промптов в эмбеддинги
- **DiT (Diffusion Transformer)**: Трансформер для денойзинга латентов
- **Causal Video VAE**: Вариационный автоэнкодер с причинными 3D-свертками
- **Rectified Flow Scheduler**: Планировщик диффузии для быстрой генерации
- **Fractional RoPE**: Нормализованное дробное позиционное кодирование

## 🔧 Сборка и разработка

### Особенности сборки

Проект использует следующие Cargo features:

- `flash-attn`: Поддержка Flash Attention 2 (экономия памяти)
- `cudnn`: Ускорение сверток через cuDNN
- `mkl`: Intel MKL для CPU-ускорения (Linux/Windows x86_64)
- `accelerate`: Apple Accelerate для Metal (macOS)
- `nccl`: Поддержка multi-GPU
- `all-gpu`: Все GPU-оптимизации

### Тестирование

```bash
# Запуск всех тестов
cargo test

# Запуск конкретного теста
cargo test --test integration

# Запуск с выводом
cargo test -- --nocapture
```

### Отладка

Для отладки используйте переменные окружения:

```bash
# Включить логирование
RUST_LOG=debug cargo run --release --bin ltx-video -- ...
```

## 🐛 Известные ограничения

- **Альфа-стадия**: Проект находится в альфа-стадии. Корректный инференс не гарантируется. Результаты могут быть нестабильными или некорректными.
- **Conv3D**: Текущая реализация использует эмуляцию 3D-сверток через 2D операции. Нативная реализация Conv3D через Custom Op может улучшить производительность.
- **Память**: Генерация видео высокого разрешения требует значительного объема GPU-памяти. Рекомендуется использовать Flash Attention и BF16.

## 📄 Лицензия

Проект распространяется под лицензией Apache-2.0. См. файл [LICENSE](LICENSE) для подробностей.

## 🙏 Благодарности

- [Hugging Face Candle](https://github.com/huggingface/candle) - Фреймворк машинного обучения на Rust
- [Lightricks LTX-Video](https://github.com/Lightricks/LTX-Video) - Оригинальная модель LTX-Video
- Сообщество разработчиков Rust и ML

---

**Примечание**: Проект находится в **альфа-стадии** и активной разработке. API может изменяться между версиями. Корректный инференс не гарантируется.

