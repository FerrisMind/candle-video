# Предкомпилированные библиотеки

[![RU English](README.RU.md)](README.RU.md)
[![EN English](README.md)](README.md)

Эта директория содержит предкомпилированные CUDA-ядра для Flash Attention, что значительно ускоряет процесс сборки.

## 📦 Содержимое

- **`libflashattention.a`** (~230 МБ) - Предкомпилированная статическая библиотека, содержащая CUDA-ядра для Flash Attention 2

## 🎯 Назначение

Компиляция CUDA-ядер для Flash Attention из исходников занимает **15-20 минут** при первой сборке. Используя предкомпилированные ядра из этой директории, последующие сборки завершаются за **секунды**.

## 🚀 Использование

Скрипт сборки `build_flash_attn.cmd` автоматически использует предкомпилированную библиотеку, если она существует:

```cmd
build_flash_attn.cmd
```

Скрипт выполнит:
1. Проверку наличия `prebuilt/libflashattention.a`
2. Копирование в директорию сборки
3. Пропуск компиляции CUDA-ядер
4. Завершение сборки за секунды

## 🔨 Создание предкомпилированной библиотеки (первый раз)

Если у вас еще нет предкомпилированной библиотеки, следуйте этим шагам:

### 1. Первоначальная сборка (компиляция CUDA-ядер)

```cmd
cargo check --lib --features flash-attn
```

Это скомпилирует CUDA-ядра (занимает 15-20 минут). Сборка может завершиться ошибкой на этапе линковки - это нормально для Windows.

### 2. Создание статической библиотеки

После компиляции создайте статическую библиотеку вручную:

```cmd
for /d %D in (target\debug\build\candle-flash-attn-*) do lib /OUT:"%D\out\libflashattention.a" "%D\out\*.o"
```

### 3. Сохранение предкомпилированной библиотеки

Скопируйте созданную библиотеку в директорию `prebuilt/`:

```cmd
mkdir prebuilt
for /d %D in (target\debug\build\candle-flash-attn-*) do copy "%D\out\libflashattention.a" prebuilt\
```

### 4. Последующие сборки

Теперь вы можете использовать быстрый скрипт сборки:

```cmd
build_flash_attn.cmd
```

## ⚠️ Важные замечания

### Git LFS

Файл `libflashattention.a` отслеживается через **Git LFS**, так как превышает лимит GitHub в 100 МБ.

**Перед клонированием репозитория:**
```bash
git lfs install
```

**Если вы уже клонировали без LFS:**
```bash
git lfs pull
```

### Совместимость платформ

⚠️ **Внимание**: Предкомпилированная библиотека скомпилирована для конкретных:
- Версии CUDA
- Архитектуры GPU (compute capability)
- Версии Visual Studio/MSVC

Если вы измените любой из этих параметров, может потребоваться пересборка библиотеки.

### Когда пересобирать

Пересоберите предкомпилированную библиотеку, если:
- Изменилась версия CUDA Toolkit
- Изменилась архитектура GPU
- Изменилась версия Visual Studio
- Возникают ошибки линковки

## 📝 Размер файла

Файл `libflashattention.a` имеет размер примерно **230 МБ**. Это нормально для статической библиотеки, содержащей скомпилированные CUDA-ядра.

## 🔗 Связанная документация

- [Скрипт сборки](../build_flash_attn.cmd) - Автоматизированный скрипт сборки с использованием предкомпилированных ядер
- [Процесс сборки](../.agent/workflows/build-flash-attn.md) - Подробные инструкции по сборке
- [Основной README](../README.md) - Обзор проекта

---

**Примечание**: Храните эту директорию в системе контроля версий (через Git LFS), чтобы делиться предкомпилированной библиотекой с другими разработчиками и системами CI/CD.

