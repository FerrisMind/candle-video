//! Wan2.1 T2V example with the same discoverable layout as `ltx-video`.
//!
//! The implementation is shared with `video-generate` so the Wan examples
//! cannot drift apart in argument parsing, device planning, or export logic.

include!("../video-generate/main.rs");
